---
interface Props {
  category: string;
  class?: string;
}

const { category, class: className = '' } = Astro.props;
---

<span class={`
  inline-block
  font-sans text-xs font-semibold uppercase tracking-widest
  text-[#111111]
  ${className}
`}>
  {category}
</span>
